/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "i.imgur.com",
      "via.placeholder.com",
      "randomuser.me",
      "static.vecteezy.com"
    ],
  },
};

export default nextConfig;

